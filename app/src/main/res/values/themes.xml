<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PamlabourShedulingandroidapp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="showactionbar" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">
        <!-- ActionBar background color -->
        <item name="colorPrimary">@color/lightbrown</item>
        <item name="colorPrimaryVariant">@color/lightbrown</item>

        <!-- Text color on the ActionBar -->
        <item name="colorOnPrimary">@color/white</item>
    </style>
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/green</item>
    </style>

    <style name="Theme.PamlabourShedulingandroidapp" parent="Base.Theme.PamlabourShedulingandroidapp" />
</resources>